/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add remote hosts here when you start embedding hotel/partner images.
    remotePatterns: [],
  },
};

export default nextConfig;
